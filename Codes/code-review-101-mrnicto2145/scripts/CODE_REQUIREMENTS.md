# 📋 Требования к Коду

  ## Обязательные Функции
  Каждый assignment.py должен содержать:

  ### 1. load_data()
  ```python
  def load_data():
          """Загружает и возвращает DataFrame"""
              pass
  ```
  Должна вернуть:
      - DataFrame с данными
      - Целевую переменную в последней колонке
      - Без пропущенных значений
      
  ### 2. target_analysis()
  ```python
  def target_analysis(df):
          """Анализирует целевую переменную"""
              pass
  ```

  Должна:
  - Вывести статистику целевой переменной
  - Показать распределение значений
  - Обработать ошибки

  ### 3. feature_statistics()
  ```python
  def feature_statistics(df):
      """Показывает статистику признаков"""
      pass
  ```

  ### 4. visualize_target()
  ```python
  def visualize_target(df):
      """Создаёт визуализацию целевой переменной"""
      pass
  ```

  Должна:
  - Создать PNG файл
  - Название файла: NN_dataset_target_distribution.png

  ### 5. visualize_features()
  ```python
  def visualize_features(df):
      """Создаёт визуализацию признаков"""
      pass
  ```

  ## Стиль Кода

  - PEP8 compliance
  - Max 120 символов на строку
  - Правильные отступы (4 пробела)
  - Docstrings для функций

  ---

  Версия: 1.0
  